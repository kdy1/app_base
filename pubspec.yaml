name: app_base
description: My own commons library for flutter.
version: 0.10.0
homepage: https://github.com/kdy1/app_base

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  typed_firestore: ^0.5.0
  cloud_firestore: ^0.14.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


name: app_base
description: My own commons library for flutter.
version: 0.9.0
homepage: https://github.com/kdy1/app_base

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  typed_firestore: ^0.4.2
  cloud_firestore: ^0.13.4+1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


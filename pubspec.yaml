name: app_base
description: My own commons library for flutter.
version: 0.8.0
homepage: https://github.com/kdy1/app_base

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  typed_firestore: ^0.4.1
  cloud_firestore: ^0.13.0+1
  firebase_analytics: ^5.0.10
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

